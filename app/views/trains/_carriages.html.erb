<% @train.carriages.ordered(@train.carriage_order).each do |carriage| %>
    <div><h2><%= link_to carriage.number.to_s, carriage, class: 'text_input'%>
      <%= link_to image_tag('carriage.png', :alt => 'train img'), carriage %>
         </h2>
    </div>
<% end %>